

<table>
	<tr>
		<td>Your name:</td>
		<td><%= text_field(:comment, :auther) %></td>
	</tr>
	<tr>
		<td>Your email:</td>
		<td><%= text_field(:comment, :auther_email) %></td>
	</tr>
	<tr>
		<td>Comment:</td>
		<td><%= text_area(:comment, :Content, :cols => 60, :rows => 20) %></td>
	</tr>
</table>
